<%- include('partials/header') -%>
<div class="container">
  <div class="justify-content-center mt-5">
    <div class="col">
      <h2><%= post.title %></h2>
      <img class="img-fluid" src="<%= post.image%>" />
      <div class="justify-content-between">
        <!-- add a 'like' to this post -->
        <!-- <form
          class="col-1"
          action="/post/likePost/<%= post.id %>?_method=PUT"
          method="POST"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form> -->
        <!-- display likes for this post -->
        <!-- <h3 class="col-3">Likes: <%= post.likes %></h3> -->
        <div class="col mt-2">
          <p><%= post.caption %></p>
        </div>
        <!-- if this post belongs to this user display a delete button -->
        <%if(post.user == user.id){ %>
        <form
          action="/post/deletePost/<%= post.id %>?_method=DELETE"
          method="POST"
          class="col"
          
        >
        <!-- original button layout with bootstrap class="btn btn-primary fa fa-trash" -->
          <button class= "btn btn-outline-danger fw-bold" type="submit">Delete This Photo</button>
        </form>
        <%}%>
        
      </div>
    </div>

<!-- add a comment to this specific post -->

    <!-- <div class="mt-5">
      <h2>Add a comment</h2>
      <form action="/comment/createComment/<%=post._id%>" method="POST">
        <div class="mb-3">
            <label for="comment" class="form-label">Comment</label>
            <input type="text" class="form-control" id="comment" name="comment">
        </div>
        
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
      
    </div> -->

<!-- display comments for this specific post -->

    <!-- <ul>
      <% for(var i=0; i<comments.length; i++) {%>
        <li class="col-6 justify-content-between mt-5">
            <%=comments[i].comment%> - By: <%=comments[i].userName%>
        </li>
        
        <%if(comments[i].user == user.id){ %>
            <li>
              <form
                action="/comment/deleteComment/<%=comments[i]._id%>&<%=post.id%>?_method=DELETE"
                method="POST"
                class="col-3"
                
              >
                <button class="btn btn-primary fa fa-trash" type="submit">Delete</button>
              </form>
            </li>
          <%}%>
        
     
      <% } %>
    </ul> -->
    <div class="col mt-5">
      <a class="btn btn-primary" href="/profile">Return to Posting Page</a>
      <a class="btn btn-primary" href="/">Return to website</a>
    </div>
  </div>
</div>

<%- include('partials/footer') -%>
